// join() trả về một chuỗi mới bằng cách nối tất cả các phần tử trong mảng này,
//  phân tách bằng dấu phẩy hoặc một chuỗi phân cách được chỉ định.

const elements = ['Fire', 'Air', 'Water'];

console.log(elements.join(' '));
// Expected output: "Fire Air Water"